# Discovered from the repository root (the action's working directory).
[workspace]
# Relative to the directory of the root Cargo.toml (proxyd/), so this is the
# repository-root CHANGELOG.md.
changelog_path = "../CHANGELOG.md"
